-- creating holberton database
-- creating user table and enforcing unique on email column
CREATE TABLE IF NOT EXISTS delete(
       id INT AUTO_INCREMENT PRIMARY KEY,
       email VARCHAR(255) NOT NULL UNIQUE,
       name VARCHAR(255)
);
